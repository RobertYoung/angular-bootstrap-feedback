language: node_js
node_js:
    - "4.2"

before_script:
    - npm install --quiet -g gulp
    - npm install
    - npm install --quiet -g bower
    - bower install
    - gulp build

script: gulp karma:unit
